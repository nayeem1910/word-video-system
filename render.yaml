



    services:
  - type: web
    name: word-video-system
    env: docker
    buildCommand: |
      mkdir -p output
      chmod -R 777 output
    startCommand: apache2-foreground
    envVars:
      - key: PORT
        value: 10000
    plan: free